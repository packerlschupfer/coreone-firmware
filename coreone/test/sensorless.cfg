# ⚠ SUPERSEDED — do not use. Use test/homing.cfg instead.
#
# This was an early Phase-2 snapshot and had accumulated several bugs:
#   - header was copy-pasted from the SPI test (described "SPI comms", not homing)
#   - missing the diag1_pushpull startup fix -> sensorless G28 completes instantly
#     with no motion (the single most important sensorless requirement)
#   - stale driver_SGT: 2 (verified values are X=1 / Y=0)
#   - X on a physical endstop (^PG2) instead of the TMC virtual_endstop
#   - wrong Z rotation_distance (4, should be 8) and the "extruder" heater pointed
#     at the bed sensor (Semitec/PA4)
#
# test/homing.cfg is the correct, verified sensorless X/Y config (right SGT, the
# diag1_pushpull fix, and live SGT-tuning notes). For the canonical full hardware
# definition see boards/xbuddy.cfg. See test/README.md.
